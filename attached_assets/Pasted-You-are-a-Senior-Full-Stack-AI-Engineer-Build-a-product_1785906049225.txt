You are a Senior Full-Stack AI Engineer. Build a production-ready AI Career Assistant web application using React (Vite + Tailwind CSS), Node.js + Express.js, Supabase (PostgreSQL + Auth + RLS), Google Gemini SDK (@google/genai), Zod, React Router, React Markdown, and Syntax Highlighter. The application must include secure authentication, protected routes, a ChatGPT-style responsive UI with dark/light mode, left sidebar for chat history, new chat, typing indicator, streaming responses (if supported), markdown rendering, code blocks, copy button, auto-scroll, and reusable components. Users can save/update resume text and job description, which are automatically sent with every chat request along with previous conversation history. Implement features including real-time AI chat, resume review, ATS optimization, interview preparation (HR and technical), skill-gap analysis, career guidance, project recommendations, certification suggestions, learning roadmap generation, LinkedIn/profile improvement, portfolio advice, and job-search guidance. Backend APIs: POST /api/chat, POST /api/chat/session, GET /api/chat/history, GET /api/chat/session/:id, DELETE /api/chat/session/:id, GET/PUT /api/profile. Create PostgreSQL tables: profiles(id, full_name, resume, job_description, timestamps), chat_sessions(id, user_id, title, created_at), and chat_messages(id, session_id, role[user|assistant], message, created_at); enable Row Level Security so users can access only their own data. Validate all requests with Zod (message max 4000 chars, resume/JD max 25000 chars, UUID validation), sanitize inputs, use Helmet, CORS, rate limiting, secure error handling, and keep the Gemini API key strictly on the backend. Use a singleton Gemini client and a system prompt instructing the AI to act as an expert Resume Reviewer, ATS Specialist, Career Coach, Technical Interviewer, and Software Engineering Mentor; always use resume and job description when available, never hallucinate missing information, state missing context clearly, and provide concise, professional, actionable responses with bullet points, strengths, weaknesses, reasoning, and next-step suggestions. Store every conversation in the database, automatically generate session titles from the first prompt, maintain chat history, return structured JSON responses, use modular architecture (components, hooks, services, controllers, middleware, validators), reusable code, loading/error states, responsive design, and production-quality code without placeholders or TODOs. Optimize the implementation for Replit AI Agent by minimizing duplicated code, unnecessary files, repeated API calls, and excessive complexity while keeping the project fully functional, secure, scalable, and ready for deployment after configuring environment variables (SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, GOOGLE_API_KEY, GEMINI_MODEL, JWT_SECRET, PORT).